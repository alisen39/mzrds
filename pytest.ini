[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: 集成测试，需要真实的 Redis 连接
    smoke: 冒烟测试和性能测试，需要真实的 Redis 连接并生成大量数据
addopts = 
    -v
    --tb=short
    -m "not integration and not smoke"

